# GET /__apis
**Operation:** `getAvailableApis` — List available REST APIs
> To retrieve the list of available REST API endpoints in your Zoho CRM organization for a specific API version. You can filter the results by module, OAuth scope, or request method using the filters parameter.

**Parameters:**
- `filters` (query, string, optional) [maxLen=2048]: This parameter allows you to filter the supported APIs for the version specified in the URL. You can filter based on three criteria: module, request method, and OAuth scope. The filter syntax is a JSON object with three fields: **field**, **comparator**, and **value**. The **field** can be one of the following: `module`, `operation_types.method`, or `operation_types.oauth_scope`. The **comparator** can be either `equals` or `contains`. The **value** is the value to filter by. Encode the filter while using it in any API platform.


**Schemas:**
`ErrorResponse`:
  > Represents the standard error response returned by the API when a request fails.
  - `code` (string) **REQ** [maxLen=64] — Returns the error code generated by the API.
  - `message` (string) **REQ** [maxLen=512] — Represents the error message describing the issue.
  - `details` (object) — Contains additional context about the error, such as the field or JSON path of the field that caused the error.
  - `status` (string) **REQ** [maxLen=64] — status

**Responses:**

- **200**: Returns the list of available REST APIs with their paths, HTTP methods, OAuth scopes, and credit consumption details. [application/json]
    > Contains the list of available REST APIs and their operation types.
    - `__apis` (array of object) [maxItems=2000] **REQ** — Contains the details of all the supported APIs in the API version given in the request URL.
      - `path` (string) **REQ** [maxLen=512] — Represents the API endpoint for the API version in the request URL.
      - `operation_types` (array of object) [maxItems=10] **REQ** — Contains the supported HTTP operations for that API in that version.

        - `method` (string) **REQ** [enum=['GET', 'POST', 'PUT', 'PATCH', 'DELETE']] — Represents the supported request method for
that API. 

Possible values:

**GET** - Retrieve data.

**POST** - Create data.

**PUT** - Update data.

**PATCH** - Partially update data.

**DELETE** - Remove data.

        - `oauth_scope` (string) **REQ** [maxLen=128] — Represents the OAuth scope required to perform this operation.
        - `max_credits` (integer/int32) **REQ** [min=0] — Represents the maximum credits that the API consumes.


        - `min_credits` (integer/int32) **REQ** [min=0] — Represents the minimum credits that the API consumes.

- **400**: The request parameter is invalid.

**Resolution:** The request must include valid filter syntax and
URL-encoded values.
 — Schema: `ErrorResponse` [application/json]
    > Represents the standard error response returned by the API when a request fails.

- **401**: Authentication failed.

**Resolution:** Pass the access token in the request header of the API
call.
 — Schema: `ErrorResponse` [application/json]
    > Represents the standard error response returned by the API when a request fails.

- **403**: Permission denied to access this resource.

**Resolution:** Contact your CRM administrator to grant the required
permission.
 — Schema: `ErrorResponse` [application/json]
    > Represents the standard error response returned by the API when a request fails.

- **404**: The requested resource does not exist.

**Resolution:** The request URL must reference a valid API version and
endpoint.
 — Schema: `ErrorResponse` [application/json]
    > Represents the standard error response returned by the API when a request fails.

- **429**: The API rate limit has been exceeded.

**Resolution:** The request should be retried after the time specified
in the **retry_after** field in the response.
 — Schema: `ErrorResponse` [application/json]
    > Represents the standard error response returned by the API when a request fails.

- **500**: An unexpected error occurred on the server.

**Resolution:** Unexpected and unhandled exception in the server.
Contact support team.
 — Schema: `ErrorResponse` [application/json]
    > Represents the standard error response returned by the API when a request fails.

**Scopes:** ZohoCRM.apis.READ
