Invalid data

```json
{
  "user_type": [
    {
      "code": "INVALID_DATA",
      "message": "Invalid data",
      "details": {
        "api_name": "read_only",
        "maximum_length": 5,
        "json_path": "$.user_type[*].modules[*].fields[*].read_only"
      },
      "status": "error"
    }
  ]
}
```
