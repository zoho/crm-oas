Invalid data

```json
{
  "user_type": [
    {
      "code": "INVALID_DATA",
      "message": "Invalid data",
      "details": {
        "api_name": "name",
        "regex": "^[A-Za-z0-9]{50}$",
        "json_path": "$.user_type[*].name"
      },
      "status": "error"
    }
  ]
}
```
