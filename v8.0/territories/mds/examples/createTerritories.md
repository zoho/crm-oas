# Examples: createTerritories

**POST /settings/territories**

## Request examples

### `application/json` — TerritoryCreate

Create a territory with name, manager, and account rule criteria

```json
{
  "territories": [
    {
      "name": "Territory1000",
      "reporting_to": {
        "id": "431581000000272796"
      },
      "manager": {
        "id": "431581000000258001"
      },
      "permission_type": "read_only",
      "description": "null",
      "account_rule_criteria": {
        "comparator": "greater_than",
        "field": {
          "api_name": "Annual_Revenue",
          "id": "431581000000000625"
        },
        "value": "1001"
      },
      "lead_rule_criteria": {
        "comparator": "greater_than",
        "field": {
          "api_name": "Annual_Revenue",
          "id": "431581000000000893"
        },
        "value": "1000"
      },
      "deal_rule_criteria": {
        "comparator": "greater_than",
        "field": {
          "api_name": "Amount",
          "id": "431581000000000799"
        },
        "value": "100000"
      }
    }
  ]
}
```

## Response examples

### Status `201` — `application/json` — AutoGeneratedExample

Successful territory creation response

```json
{
  "territories": [
    {
      "code": "SUCCESS",
      "details": {
        "id": "431581000000734292"
      },
      "message": "Territory Created Successfully",
      "status": "success"
    }
  ]
}
```

### Status `400` — `application/json` — MandatoryNotFoundReportingTo

Mandatory field reporting_to missing in request

```json
{
  "territories": [
    {
      "code": "MANDATORY_NOT_FOUND",
      "details": {
        "api_name": "reporting_to",
        "json_path": "$.territories[0].reporting_to"
      },
      "message": "required field not found",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — MandatoryNotFoundName

Mandatory field name missing in request

```json
{
  "territories": [
    {
      "code": "MANDATORY_NOT_FOUND",
      "details": {
        "api_name": "name",
        "json_path": "$.territories[0].name"
      },
      "message": "required field not found",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — MandatoryNotFoundPermissionType

Mandatory field permission_type missing in request

```json
{
  "territories": [
    {
      "code": "MANDATORY_NOT_FOUND",
      "details": {
        "api_name": "permission_type",
        "json_path": "$.territories[0].permission_type"
      },
      "message": "required field not found",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — TerritoryMaximumLimit

Territory maximum limit reached

```json
{
  "territories": [
    {
      "code": "LIMIT_EXCEEDED",
      "details": {
        "limit": 250
      },
      "message": "Territory Limit reached",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — CriteriaInvalidFieldDataType

Invalid field data type in criteria

```json
{
  "territories": [
    {
      "code": "INVALID_DATA",
      "details": {
        "api_name": "field",
        "json_path": "$.territories[0].account_rule_criteria.field",
        "expected_data_type": "jsonobject"
      },
      "message": "invalid data",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — CriteriaFieldComparatorMismatch

Field and comparator mismatch in criteria

```json
{
  "territories": [
    {
      "code": "DEPENDENT_MISMATCH",
      "details": {
        "dependee": {
          "api_name": "field",
          "json_path": "$.territories[0].account_rule_criteria.field"
        },
        "api_name": "comparator",
        "json_path": "$.territories[0].account_rule_criteria.comparator",
        "supported_values": [
          "equal",
          "not_equal",
          "greater_than",
          "less_than"
        ]
      },
      "message": "The comparator is not compatible with the given field",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — CriteriaFieldValueMismatch

Field value type mismatch in criteria

```json
{
  "territories": [
    {
      "code": "DEPENDENT_MISMATCH",
      "details": {
        "dependee": {
          "api_name": "field",
          "json_path": "$.territories[0].account_rule_criteria.field"
        },
        "api_name": "value",
        "json_path": "$.territories[0].account_rule_criteria.value",
        "expected_data_type": "integer"
      },
      "message": "The value is not compatible with the given field",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — CriteriaFieldComparatorValueMissing

Dependent field missing in criteria

```json
{
  "territories": [
    {
      "code": "DEPENDENT_FIELD_MISSING",
      "details": {
        "dependee": {
          "api_name": "field",
          "json_path": "$.territories[0].account_rule_criteria.field"
        },
        "api_name": "comparator",
        "json_path": "$.territories[0].account_rule_criteria.comparator"
      },
      "message": "required field not found",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — CriteriaFieldNotSupported

Field not supported in territory rule criteria

```json
{
  "territories": [
    {
      "code": "NOT_SUPPORTED",
      "details": {
        "api_name": "File_Upload",
        "json_path": "$.territories[0].account_rule_criteria.field.api_name"
      },
      "message": "The given field api_name is not supported in criteria",
      "status": "error"
    }
  ]
}
```

### Status `400` — `application/json` — TerritoryNotYetEnabled

Territory management not yet enabled error

```json
{
  "code": "FEATURE_NOT_ENABLED",
  "details": {},
  "message": "Territory Management is not enabled",
  "status": "error"
}
```

### Status `400` — `application/json` — TerritoryDisabled

Territory feature disabled error

```json
{
  "code": "FEATURE_NOT_ENABLED",
  "details": {},
  "message": "Territory Management is disabled",
  "status": "error"
}
```

### Status `401` — `application/json` — AuthenticationFailure

Authentication failure error

```json
{
  "code": "AUTHENTICATION_FAILURE",
  "details": {},
  "message": "Authentication failed",
  "status": "error"
}
```
