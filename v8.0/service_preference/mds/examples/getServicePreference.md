# Examples: getServicePreference

**GET /settings/service_preferences**

## Response examples

### Status `200` — `application/json` — SuccessResponse

An example successful response for retrieving service preferences.

```json
{
  "service_preferences": {
    "job_sheet_enabled": true
  }
}
```
