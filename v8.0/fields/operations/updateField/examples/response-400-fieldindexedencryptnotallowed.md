Indexed field cannot be encrypted

```json
{
  "fields": [
    {
      "code": "NOT_ALLOWED",
      "message": "Indexed field cannot be encrypted",
      "details": {
        "api_name": "crypt",
        "json_path": "$.fields[0].crypt"
      },
      "status": "error"
    }
  ]
}
```
